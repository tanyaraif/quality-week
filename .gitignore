# Локальные секреты и переменные окружения
.env
.env.local
.mcp.json
# На случай, если токен снова окажется в .mcp.json — не коммитим его
# (сейчас токен подставляется из переменной окружения SUPABASE_ACCESS_TOKEN)
